/-Page to view all modules

= render "shared/backgroundAnimation"

.jumbotron.text-center
  %h1= t('.title')


%table
  %thead
    %tr
      %th= t('.module_title')
      %th= t('.module_code')
      %th= t('.module_overview')
      %th= t('.module_leader')
      %th= t('.credits')
      %th= t('.year')
      %th
      %th

  %tbody
    - @course_modules.each do |course_module|
      %tr
        %td= course_module.Title
        %td= course_module.ModuleCode
        %td= course_module.ModuleOverview
        %td= course_module.ModuleLeader
        %td= course_module.Credits
        %td= course_module.Year
        %td= link_to t('.show_button'), course_module #allow user to view details about the module
        %td= link_to t('.select_button'), new_selected_module_path(:module_id => course_module) #allow the user to select the module

%br
